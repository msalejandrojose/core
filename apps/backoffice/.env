# Variables NO-secretas del backoffice. SE COMMITEA.
# Para secretos o overrides locales usa .env.local (gitignored).

VITE_API_URL=http://localhost:3000

# URL base pública de formularios (web #94). Opcional; fallback al origin actual.
VITE_PUBLIC_FORMS_URL=http://localhost:4321
