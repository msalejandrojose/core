// Placeholder de /login. BO-02 implementa el formulario real con
// react-hook-form + zod + llamada al endpoint /auth/login.

export function LoginPage() {
  return (
    <main style={{ padding: 24 }}>
      <h1>Login</h1>
      <p>Pantalla de autenticación — implementación en BO-02.</p>
    </main>
  );
}
