// Categoría base fija de un Site. Mantiene el mapa siempre filtrable aunque
// las etiquetas libres (Tag) se fragmenten.
export type SiteCategory = 'RESTAURANT' | 'NATURE' | 'CULTURE' | 'LEISURE' | 'OTHER';
